{
  "name": "cloudoptimizer-backend",
  "version": "1.0.0",
  "description": "CloudOptimizer API Server - Auth, orchestration, cloud provider integration",
  "main": "src/server.js",
  "scripts": {
    "dev": "node src/server.js",
    "start": "node src/server.js",
    "test": "node -e \"require('fs').readdirSync('src',{recursive:true}).filter(f=>f.endsWith('.js')).forEach(f=>{require('child_process').execFileSync(process.execPath,['--check','src/'+f]);console.log('ok  src/'+f)})\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "jsonwebtoken": "^9.0.2",
    "bcryptjs": "^2.4.3",
    "axios": "^1.6.2",
    "uuid": "^9.0.0"
  }
}
