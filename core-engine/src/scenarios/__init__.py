"""
Scenario integration layer for the CloudOptimizer core engine.

Bridges the standalone scenario test suite at /scenarios (which can be run
directly via runner.py) with the FastAPI core engine endpoints that serve
scenario analysis results to the frontend dashboard.
"""
